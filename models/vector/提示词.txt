================================================================================
models/vector/ 目录代码生成提示词合集
================================================================================
本文件汇总 models/vector/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
向量模型：文档/案例向量记录的元数据定义（embedding 由向量库存储）。

【依赖】
- from models.vector.case_vectors import CaseVector
- from models.vector.document_vectors import DocumentVector

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】case_vectors.py
================================================================================
你是一名 Python 后端开发者。请实现 case_vectors.py。

【背景】
事故/案例类向量记录模型（案例检索的知识库单元）。 案例向量供安全审核智能体的 case_retriever 使用： 输入同类工况描述，检索历史事故案例辅助风险评估。

【依赖】
- from __future__ import annotations
- from datetime import datetime
- from typing import Any
- from pydantic import BaseModel, Field
- from common.ids import new_id
- from common.timeutils import utc_now

【必须实现的类】
1. class CaseVector(BaseModel)：一条事故/工程案例的向量记录元数据。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 3】document_vectors.py
================================================================================
你是一名 Python 后端开发者。请实现 document_vectors.py。

【背景】
法规/标准/方案类文档的向量记录模型（ChromaDB/Pinecone 通用）。 一条 DocumentVector 对应文档的一个切块（chunk）， embedding 本身不在此模型中——由向量库存储，本模型承载检索所需的元数据。

【依赖】
- from __future__ import annotations
- from datetime import datetime
- from typing import Any
- from pydantic import BaseModel, Field
- from common.constants import DocType
- from common.ids import new_id
- from common.timeutils import utc_now

【必须实现的类】
1. class DocumentVector(BaseModel)：文档切块的向量记录元数据。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================