# 5c.5 预留：K8s Ingress — 外部路由入口。
# 模板：
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: caai-ingress
#   namespace: caai
#   annotations:
#     nginx.ingress.kubernetes.io/ssl-redirect: "true"
# spec:
#   tls:
#     - hosts: [caai.example.com]
#       secretName: caai-tls
#   rules:
#     - host: caai.example.com
#       http:
#         paths:
#           - path: /api
#             pathType: Prefix
#             backend:
#               service:
#                 name: caai-api
#                 port:
#                   number: 80
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: caai-streamlit
#                 port:
#                   number: 80
