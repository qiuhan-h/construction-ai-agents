# 5c.5 预留：K8s Service — Streamlit 前端暴露。
# 模板：
# apiVersion: v1
# kind: Service
# metadata:
#   name: caai-streamlit
#   namespace: caai
# spec:
#   selector:
#     app: caai-streamlit
#   ports:
#     - port: 80
#       targetPort: 8501
#       protocol: TCP
