================================================================================
core/vector_store/ 目录代码生成提示词合集
================================================================================
本文件汇总 core/vector_store/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
core.vector_store 包级 API（5b.2 实做）。 设计： - 5b.2 把 ``agents/safety_audit_agent/knowledge_base/vector_store.py`` 的 抽象（VectorStore / VectorRecord / VectorHit）下沉到 ``core/vector_store``， 解除 ``core`` → ``agents`` 的反向依赖； - ``agents/.../vector_store.py`` 通过 re-export 保持向后兼容（4d 业务不变）； - 后端双实现： - ``InMemoryVectorStore``  ‒ 纯 Python，无第三方依赖（沙箱 / 测试用） - ``ChromaVectorStore``    ‒ ChromaDB 持久化（生产；缺包时自动降级到 InMemory） - 通过 ``factory.get_vector_store(backend)`` 选实现； - 5b.2 不改 ``scripts/setup_vector_db.py`` 的接口（依然返回 (store, VectorRecord)）。 包结构： core/vector_store/ __init__.py     re-export 统一 API base.py         Protocol + VectorRecord / VectorHit / 异常 in_memory.py    内存版（从 agents/.../vector_store.py 迁移，逻辑不变） chroma_store.py ChromaDB 版（缺包时降级 InMemory） factory.py      get_vector_store(backend)

【依赖】
- from __future__ import annotations
- from core.vector_store.base import VectorHit, VectorRecord, VectorStore, VectorStoreError
- from core.vector_store.factory import list_backends, reset_default_vector_store, get_vector_store

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】base.py
================================================================================
你是一名 Python 后端开发者。请实现 base.py。

【背景】
向量库抽象（5b.2）。 设计： - ``VectorStore`` Protocol：4 个核心方法 ``upsert`` / ``query`` / ``delete`` / ``count``； - ``VectorRecord`` / ``VectorHit``：dataclass，字段与 4d 时期 ``agents/safety_audit_agent/knowledge_base/vector_store.py`` 完全一致， 业务层可平滑迁移； - ``VectorStoreError``：包级异常，所有实现抛此异常的子类（NotImplementedError 保留，用于"未实现"语义区分）。 - 不引入第三方依赖（typing.Protocol 已足够，3.8+）。

【依赖】
- from __future__ import annotations
- from dataclasses import dataclass, field
- from typing import Any, Iterable, Protocol, runtime_checkable

【必须实现的类】
1. class VectorStoreError(Exception)：所有 vector_store 异常的基类。
1. class BackendUnavailableError(VectorStoreError)：期望的后端不可用（如 chromadb 未安装且未允许降级）。
1. class VectorRecord：一条向量记录。 字段与 4d 时期完全一致，便于业务层向后兼容： - ``id``        唯一 ID（reg/code/version/case_id 等） - ``tenant_id`` 租户 ID（多租户隔离） - ``text``      原文（用于回显 / 兜底） - ``vector``    向量（可选；空时由 store 自动算） - ``metadata``  任意附加字段
1. class VectorHit：一条检索结果。
1. class VectorStore(Protocol)：向量库统一协议。
   方法：
   - async upsert(self, records: list[VectorRecord]) -> int  # 写入或更新 records。返回写入条数（与 len(records) 相等，失败抛异常）。
   - async query(self, tenant_id: str, text: str, top_k: int, filter: dict[str, Any] | None, min_score: float) -> list[VectorHit]  # 按文本检索（异步）。返回 top_k 命中，按 score 降序。
   - async delete(self, ids: Iterable[str]) -> int  # 按 id 删除。返回实际删除条数。
   - count(self, tenant_id: str | None=None) -> int  # 记录条数（按租户可选过滤）。同步：用于运维 / 监控。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 3】chroma_store.py
================================================================================
你是一名 Python 后端开发者。请实现 chroma_store.py。

【背景】
ChromaDB 向量库（5b.2 实做）。 设计： - 包 ``chromadb`` 缺失时，**不抛错**而是 fallback 到 ``InMemoryVectorStore`` 并打 WARNING；这是 5b.2 F2（后端生产化不破坏 4d 接口契约）的关键： 即使生产环境没装 chromadb，业务层仍可用； - 包存在时尝试 ``PersistentClient(path=persist_dir)`` 落盘； - 客户端初始化失败（版本不兼容 / 端口占用）→ 同样 fallback； - 与 ``InMemoryVectorStore`` 接口 100% 一致； - ``upsert`` 写入时把 ``vector`` 传给 chromadb（无 vector 时跳过 ``embeddings`` 字段，让 chromadb 用 embedding function）； - ``query`` 用 ``tenant_id`` + ``filter`` 实现多租户隔离 + 元数据过滤； - 评分：chroma 返回 ``distance``，本类转 ``score = 1 / (1 + distance)``， 使之与 InMemory 的 cosine 一样数值越大越相关。

【依赖】
- from __future__ import annotations
- import asyncio
- import logging
- import os
- import threading
- from typing import Any, Iterable
- from core.vector_store.base import BackendUnavailableError, VectorHit, VectorRecord, VectorStoreError
- from core.vector_store.in_memory import InMemoryVectorStore

【必须实现的类】
1. class ChromaVectorStore：ChromaDB 持久化向量库（生产首选）。 属性： - ``backend_name`` = ``"chroma"`` - 缺包 / 初始化失败 → ``InMemoryVectorStore``（通过 ``_fallback``）
   方法：
   - __init__(self, persist_dir: str, collection_name: str, allow_fallback: bool) -> None
   - _init_backend(self) -> None
   - _fallback_or_raise(self, reason: str) -> None
   - async upsert(self, records: list[VectorRecord]) -> int
   - _upsert_sync(self, records: list[VectorRecord]) -> int
   - async query(self, tenant_id: str, text: str, top_k: int, filter: dict[str, Any] | None, min_score: float) -> list[VectorHit]
   - _query_sync(self, tenant_id: str, text: str, top_k: int, filter: dict[str, Any] | None, min_score: float) -> list[VectorHit]
   - async delete(self, ids: Iterable[str]) -> int
   - _delete_sync(self, ids: list[str]) -> int
   - count(self, tenant_id: str | None=None) -> int

【必须实现的函数】
1. _chroma_available() -> bool  # 探测 chromadb 是否可用。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 4】factory.py
================================================================================
你是一名 Python 后端开发者。请实现 factory.py。

【背景】
VectorStore 工厂（5b.2）。 设计： - ``get_vector_store(backend=None)``： - ``None``（默认）→ 读环境变量 ``VECTOR_STORE_BACKEND``；未设或 ``"memory"`` / ``"in_memory"`` → InMemory；``"chroma"`` → Chroma； - 显式 ``"memory"`` / ``"in_memory"`` / ``"chroma"`` → 直接选后端； - ``list_backends()`` → 返回当前进程已注册的后端名（用于健康检查 / 运维）； - ``reset_default_vector_store()`` → 测试用，重置单例； - 工厂**永不抛 ImportError**——后端缺失一律降级为 InMemory； 业务层只需 ``store = get_vector_store()``。

【依赖】
- from __future__ import annotations
- import logging
- import os
- import threading
- from typing import Any, Literal
- from core.vector_store.base import VectorStore

【关键常量】
- _VALID = {'memory', 'in_memory', 'chroma'}

【必须实现的函数】
1. _normalize(name: str | None) -> str
2. get_vector_store(backend: str | None=None) -> VectorStore  # 获取向量库实例（进程级单例，按 backend 缓存）。 参数： - ``backend``： - ``None``     → 读环境变量 ``VECTOR_STORE_BACKEND`` - ``"memory"`` → InMemoryVectorStore - ``"chroma"`` → ChromaVectorStore（缺 chromadb 时自动 fallback） 切换 backend 不会丢失旧实例：每个 backend 各自缓存一份单例， 切回原 backend 仍返回原实例（含已写入数据）。
3. list_backends() -> list[str]  # 返回后端能力列表（含缺包标记）。
4. reset_default_vector_store() -> None  # 重置单例（测试 / 重启场景）。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 5】in_memory.py
================================================================================
你是一名 Python 后端开发者。请实现 in_memory.py。

【背景】
InMemoryVectorStore（5b.2 迁移版）。 来源： - 4d 时期位于 ``agents/safety_audit_agent/knowledge_base/vector_store.py``； - 5b.2 把它迁到 ``core/vector_store/in_memory.py``，并实现新协议 ``core.vector_store.base.VectorStore``。 行为： - 词袋 + 词频 → L2 归一化（与 4d 完全一致，业务层检索结果不变）； - 多租户严格隔离（query 时 ``r.tenant_id != tenant_id`` 直接跳过）； - ``upsert`` 同步路径放线程池，避免阻塞事件循环。

【依赖】
- from __future__ import annotations
- import asyncio
- import math
- import re
- import threading
- from collections import Counter
- from typing import Any, Iterable
- from core.vector_store.base import VectorHit, VectorRecord, VectorStoreError

【关键常量】
- _TOKEN_PATTERN

【必须实现的类】
1. class InMemoryVectorStore：内存版向量库（无第三方依赖；沙箱 / 测试用）。
   方法：
   - __init__(self) -> None
   - async upsert(self, records: list[VectorRecord]) -> int
   - async query(self, tenant_id: str, text: str, top_k: int, filter: dict[str, Any] | None, min_score: float) -> list[VectorHit]
   - async delete(self, ids: Iterable[str]) -> int
   - count(self, tenant_id: str | None=None) -> int
   - _upsert_sync(self, records: list[VectorRecord]) -> int
   - _query_sync(self, tenant_id: str, text: str, top_k: int, filter: dict[str, Any] | None, min_score: float) -> list[VectorHit]
   - _delete_sync(self, ids: list[str]) -> int
   - all_records(self, tenant_id: str | None=None) -> list[VectorRecord]

【必须实现的函数】
1. _tokenize(text: str) -> list[str]
2. _to_vector(tokens: list[str]) -> list[float]
3. _cosine(a: list[float], b: list[float]) -> float
4. _match_filter(metadata: dict[str, Any], flt: dict[str, Any]) -> bool

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================