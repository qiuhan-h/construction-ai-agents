# 已废弃：5c.1 OpenTelemetry 实现已迁至 core/observability/tracer.py（get_tracer + setup）。
# 旧命名 tracing.py 与 stage5_plan.txt §1.3 要求的文件名不一致，已由 tracer.py 替代。
# verify_stage5.py 探针 _probe_otel_tracer 检查的是 tracer.py，不引用本文件。
# 阶段六可安全删除。
