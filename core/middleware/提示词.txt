================================================================================
core/middleware/ 目录代码生成提示词合集
================================================================================
本文件汇总 core/middleware/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
core/middleware 子包：HTTP 中间件适配层（7a-5 安全加固）。 当前导出： - AuditLogMiddleware：安全审计日志中间件（记录写操作审计轨迹）

【依赖】
- from core.middleware.audit_middleware import AuditLogMiddleware

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】audit_middleware.py
================================================================================
你是一名 Python 后端开发者。请实现 audit_middleware.py。

【背景】
审计日志中间件（7a-5 安全加固）。 记录 API 请求的审计轨迹：tenant_id + actor + action + resource + status_code。 生产环境合规审计与安全追溯使用。 设计： - 仅记录写操作（POST/PUT/PATCH/DELETE）和登录操作，减少日志量； - 数据库写入在后台线程执行，不阻塞响应； - 数据库不可用时降级为 logger 记录。

【依赖】
- from __future__ import annotations
- import asyncio
- import logging
- import uuid
- from typing import Any
- from starlette.middleware.base import BaseHTTPMiddleware
- from starlette.requests import Request
- from starlette.responses import Response

【必须实现的类】
1. class AuditLogMiddleware(BaseHTTPMiddleware)：审计日志中间件：记录写操作和登录操作的审计轨迹。
   方法：
   - async dispatch(self, request: Request, call_next)
   - _determine_action(self, method: str, path: str) -> str  # 根据HTTP方法和路径确定操作类型。
   - async _record_audit_log(self, **kwargs) -> None  # 异步记录审计日志到数据库；失败降级为日志。
   - _write_to_db(self, data: dict[str, Any]) -> None  # 同步写入数据库。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================