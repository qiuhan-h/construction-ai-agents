================================================================================
core/gis/ 目录代码生成提示词合集
================================================================================
本文件汇总 core/gis/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
core.gis：GIS 集成层顶层入口（占位）。 状态：5b 阶段占位未实装。实际 GIS 功能在 ``agents/site_monitor_agent/gis_monitoring/`` 已实现（geofencing / spatial_monitor 等）。 5c 阶段计划： - ``engine.py``：GIS 引擎基类（坐标变换 / 空间索引 / 图层管理） - ``services/coordinate_transform.py``：WGS84 ↔ GCJ-02 ↔ BD-09 - ``services/geocoding.py``：地址 ↔ 坐标 - ``services/routing.py``：路径规划 - ``layers/``：安全 / 环境 / 施工图层 当前请直接从 ``agents.site_monitor_agent.gis_monitoring`` 导入 GIS 工具。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】engine.py
================================================================================
你是一名 Python 后端开发者。请实现 engine.py。

【背景】
GIS 引擎基类（5b 占位，5c 实装）。 计划接口（5c）： class GISEngine: def transform_coord(self, lng: float, lat: float, *, from_crs: str, to_crs: str) -> tuple[float, float]: ... def spatial_index(self, points: list[GeoPoint]) -> SpatialIndex: ... def add_layer(self, layer: GeoLayer) -> None: ... def query_layers(self, bbox: BBox) -> list[GeoLayer]: ... 当前 GIS 引擎能力在 ``agents.site_monitor_agent.gis_monitoring/`` 的 ``geofencing.py`` / ``spatial_monitor.py`` 中以 agent 级实现提供。 core 层抽象将在 5c 可观测 + GIS 集成阶段补齐。

【依赖】
- from __future__ import annotations

【必须实现的类】
1. class GISEngine：GIS 引擎基类（占位）。5c 阶段实装。
   方法：
   - __init__(self) -> None

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 3】map_visualization.py
================================================================================
你是一名 Python 后端开发者。请实现 map_visualization.py。

【背景】
地图可视化工具（5b 占位，5c 实装）。 计划：地图渲染、图层叠加、标注生成等。 当前可视化能力在 ``streamlit_app/components/chart_component.py``。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 4】spatial_analysis.py
================================================================================
你是一名 Python 后端开发者。请实现 spatial_analysis.py。

【背景】
空间分析工具（5b 占位，5c 实装）。 计划：空间索引、缓冲区分析、空间聚类、热力图等。 当前空间分析能力在 ``agents.site_monitor_agent.gis_monitoring/spatial_monitor.py``。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================