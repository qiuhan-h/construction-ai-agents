================================================================================
core/orchestrator/ 目录代码生成提示词合集
================================================================================
本文件汇总 core/orchestrator/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
core/orchestrator 包级 re-export。 公开 API： - Orchestrator：主编排器 - WorkflowEngine / Workflow / TaskSpec：工作流描述与执行 - TaskScheduler：任务调度（Celery 入队） - CollaborationManager：多智能体协作时间线 - HumanReviewServiceAdapter：人机交互适配 - AgentManagerProtocol：4c 协议，4d 实现

【依赖】
- from core.orchestrator.collaboration_manager import CollaborationEntry, CollaborationManager
- from core.orchestrator.human_interaction import HumanReviewServiceAdapter, get_human_review_adapter, reset_human_review_adapter
- from core.orchestrator.orchestrator import Orchestrator
- from core.orchestrator.task_scheduler import MAX_RETRIES, TaskScheduler, TaskSchedulerError, register_celery_tasks
- from core.orchestrator.workflow_engine import AgentManagerProtocol, TaskSpec, Workflow, WorkflowEngine

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】a2a_handlers.py
================================================================================
你是一名 Python 后端开发者。请实现 a2a_handlers.py。

【背景】
编排器 A2A handler：让业务智能体可以通过 A2A 反向调用编排器（触发工作流 / 查任务）。 设计原则（O1, O5）： - 编排器本身以 A2AAgentProtocol 形态注册到 AgentRegistry； - 业务 Agent 可通过 `orchestrator.send_message`（method=agent.send_message）触发工作流； - 4c 暴露 3 个 method： - agent.send_message   通用入口：metadata 携带 workflow_name 触发工作流 - workflow.list        列出已注册工作流 - workflow.get_run     按 run_id 取执行结果 依赖： - 阶段一 common.ids / common.timeutils - 阶段二 core.a2a.message / core.a2a.server - 阶段四 core.orchestrator.orchestrator

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from common.ids import new_id
- from common.timeutils import to_iso, utc_now
- from core.a2a.message import A2AMessage, Artifact, Task
- from core.a2a.server import A2AAgentProtocol, get_registry

【必须实现的类】
1. class OrchestratorA2AHandler(A2AAgentProtocol)：编排器 A2A handler：把编排器的能力通过 A2A 协议对外暴露。
   方法：
   - __init__(self, orchestrator: Any) -> None
   - async handle(self, message: A2AMessage, task: Task) -> A2AMessage  # 分发到对应 method。
   - async get_task(self, task_id: str) -> Task | None
   - async cancel_task(self, task_id: str, reason: str | None=None) -> bool
   - list_tasks(self) -> list[Task]
   - async _handle_send_message(self, message: A2AMessage, task: Task) -> dict[str, Any]  # metadata.workflow_name 必填；payload 由 message.parts 提取。
   - async _handle_workflow_list(self, message: A2AMessage) -> dict[str, Any]
   - async _handle_workflow_get_run(self, message: A2AMessage) -> dict[str, Any]

【必须实现的函数】
1. _build_orchestrator_card() -> Any  # 构造编排器的 Agent Card（最小可用，4c+ 可改为从 yaml 加载）。
2. _reply(src: A2AMessage, role: str, data: dict) -> A2AMessage
3. register_orchestrator_a2a(orchestrator: Any) -> OrchestratorA2AHandler  # 把编排器注册为 A2A 智能体；返回 handler 便于测试。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 3】collaboration_manager.py
================================================================================
你是一名 Python 后端开发者。请实现 collaboration_manager.py。

【背景】
多智能体协作时间线。 设计原则（O5）： - 记录'谁跟谁交互 / 何时 / 状态'； - 默认内存 dict；4c+ 可注入 persist_path 落 JSON； - 多租户硬隔离：record_interaction 必须带 tenant_id（在调用方强约束）。

【依赖】
- from __future__ import annotations
- import json
- import logging
- import os
- from dataclasses import dataclass, field
- from typing import Any
- from common.timeutils import to_iso, utc_now

【必须实现的类】
1. class CollaborationEntry：一次协作交互记录。
   方法：
   - to_dict(self) -> dict
1. class CollaborationManager：多智能体协作时间线（task_id → 交互序列）。
   方法：
   - __init__(self, storage: dict | None=None, persist_path: str | None=None) -> None
   - record_interaction(self, from_agent: str, to_agent: str, task_id: str, status: str, tenant_id: str, metadata: dict | None) -> None  # 记录一次交互。
   - get_timeline(self, task_id: str) -> list[dict]  # 获取某个任务的全量交互时间线。
   - get_timeline_by_tenant(self, tenant_id: str) -> list[dict]  # 按租户聚合时间线（多任务汇总）。
   - list_task_ids(self) -> list[str]
   - count(self, task_id: str | None=None) -> int
   - clear(self) -> None
   - _flush_to_disk(self) -> None
   - _load_from_disk(self) -> None

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 4】human_interaction.py
================================================================================
你是一名 Python 后端开发者。请实现 human_interaction.py。

【背景】
人机交互适配层：编排器 ↔ 人工复核服务。 设计原则（O5）： - 适配器只负责把编排器侧的语义映射到 services/human_review_service； - 不在适配层持久化，所有存储由 human_review_service 负责； - 多租户硬隔离：所有接口必传 tenant_id。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any

【必须实现的类】
1. class HumanReviewServiceAdapter：编排器 ↔ 人工复核服务适配层。
   方法：
   - __init__(self, service: Any | None=None) -> None
   - _get_service(self) -> Any
   - async request_human_review(self, tenant_id: str, task_id: str, reason: str, context: dict) -> str  # 发起人工复核请求，返回 request_id。
   - async list_pending(self, tenant_id: str) -> list[dict]
   - async approve(self, request_id: str, reviewer: str, tenant_id: str, comment: str) -> bool
   - async reject(self, request_id: str, reviewer: str, tenant_id: str, comment: str) -> bool

【必须实现的函数】
1. get_human_review_adapter() -> HumanReviewServiceAdapter  # 获取默认适配器（单例）。
2. reset_human_review_adapter() -> None  # 重置默认适配器（测试用）。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 5】orchestrator.py
================================================================================
你是一名 Python 后端开发者。请实现 orchestrator.py。

【背景】
主编排器：订阅事件 → 触发工作流 → 调度任务 → 协调多智能体协作。 设计原则（O1, O2, O5, O6, O7）： - 编排器只通过 A2A 调度智能体； - 订阅 4 类业务事件（alert/inspection/violation/regulation），按规则触发工作流； - 所有触发带 tenant_id，硬隔离； - 失败显式化（记录到 collaboration_manager 时间线 + 返回 run_id）。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from common.ids import new_id
- from common.timeutils import utc_now
- from core.events import TOPIC_ALERT_TRIGGERED, TOPIC_INSPECTION_COMPLETED, TOPIC_REGULATION_UPDATED, TOPIC_VIOLATION_CREATED, Event, get_event_bus
- from core.orchestrator.collaboration_manager import CollaborationManager
- from core.orchestrator.workflow_engine import AgentManagerProtocol, Workflow, WorkflowEngine

【必须实现的类】
1. class Orchestrator：主编排器。
   方法：
   - __init__(self, task_queue: Any, agent_manager: AgentManagerProtocol, workflow_engine: WorkflowEngine, collaboration: CollaborationManager | None, event_bus: Any | None) -> None
   - async start(self) -> None  # 订阅所有 TOPIC_* 事件 → 映射到工作流。
   - async stop(self) -> None
   - async trigger_workflow(self, name: str, payload: dict, tenant_id: str) -> str  # 手动触发工作流，返回 workflow_run_id。 4e 起：手动路径也写入协作时间线，dashboard 才能看到操作记录。 失败不影响主流程，仅 warning 日志。
   - list_workflows(self) -> list[dict]
   - get_run(self, run_id: str) -> dict | None
   - stats(self, tenant_id: str | None=None) -> dict  # 编排器运行统计（4e dashboard 使用）。 返回： - workflows: int 已注册工作流数 - runs: int 内存中的运行总数（按 tenant 过滤时仅统计该租户） - today_triggers: int 今日（按 ts 日期前缀匹配）触发次数 - failed: int 状态为 failed 的运行数 - last_run_id: str 最近一次运行 id（按 _runs dict 插入序） - last_run_status: str 最近一次运行状态
   - async _on_alert(self, event: Event) -> None  # 告警事件 → critical 时启动安全审查工作流。
   - async _on_inspection(self, event: Event) -> None  # 审查完成事件 → 不通过/有条件通过时启动合规复审。
   - async _on_violation(self, event: Event) -> None  # 违规新建 → 记录 + 通知（4c 通过 system 动作发通知，4d API 路由也可触发）。
   - async _on_regulation(self, event: Event) -> None  # 法规更新 → 记录 + 通知。
   - is_running(self) -> bool
   - workflow_engine(self) -> WorkflowEngine
   - collaboration(self) -> CollaborationManager

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 6】task_scheduler.py
================================================================================
你是一名 Python 后端开发者。请实现 task_scheduler.py。

【背景】
任务调度：把 TaskSpec 提交到 TaskQueue（Celery），管理状态查询/取消。 设计原则（O3, O7）： - task_scheduler 不直接执行 agent，只负责入队 / 优先级 / 重试； - 实际执行交给 Celery worker（worker 在 4d start_worker.py 启动）； - 任务名格式：orchestrator.<workflow_name>.<node_id>。 - 重试策略：指数退避，最多 3 次，base 1.0s（4c+ 可由配置覆盖）。

【依赖】
- from __future__ import annotations
- import asyncio
- import logging
- from typing import Any
- from common.exceptions import AppException
- from common.ids import new_id
- from core.orchestrator.workflow_engine import TaskSpec

【关键常量】
- MAX_RETRIES = 3
- BASE_BACKOFF_SECONDS = 1.0

【必须实现的类】
1. class TaskSchedulerError(AppException)：任务调度异常。
1. class TaskScheduler：任务调度（提交 / 状态 / 取消）。
   方法：
   - __init__(self, queue: Any) -> None
   - async submit(self, task: TaskSpec, tenant_id: str, priority: int, run_id: str | None, workflow_name: str | None) -> str  # 提交到队列，返回 celery task_id（mock 模式下返回 mock-*）。
   - async get_status(self, task_id: str) -> dict
   - async cancel(self, task_id: str) -> bool
   - _build_task_name(workflow_name: str | None, task: TaskSpec) -> str

【必须实现的函数】
1. register_celery_tasks(celery_app: Any) -> None  # 把 orchestrator 节点包装成 Celery task 注册到 celery_app。 worker 端执行流程： 1) 反序列化 params + tenant_id + run_id + node_id 2) 通过 agent_manager 拿 agent 3) 调 agent.handle(msg, task) 4) 发布 TOPIC_AGENT_TASK_DONE 事件 依赖注入方式： - agent_manager 通过 celery_app.conf["agent_manager"] 在 worker 启动时设置。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 7】workflow_engine.py
================================================================================
你是一名 Python 后端开发者。请实现 workflow_engine.py。

【背景】
工作流引擎：DAG 描述 + 模板渲染 + 拓扑排序 + 并行派发。 设计原则（O1-O2）： - 编排器只通过 A2A 调度智能体（节点 = agent 调用），或 system 动作。 - 节点依赖 = depends_on 列表；执行时分层并行。 - params 支持 ${var} 占位符 → 渲染自 payload，缺失抛 ValueError。 - 节点重试由调用方决定（task_scheduler 负责指数退避）。 依赖： - 阶段一 common.ids（task_id / message_id） - 阶段二 core.a2a（A2AMessage/MessagePart/Task） - 阶段二 core.events（不直接使用，但需要 EventBus 实例在 Orchestrator 层）

【依赖】
- from __future__ import annotations
- import asyncio
- import logging
- from dataclasses import dataclass, field
- from typing import Any, Literal, Protocol
- from common.ids import message_id, new_id, task_id
- from core.a2a.message import A2AMessage, MessagePart, Task

【必须实现的类】
1. class TaskSpec：工作流单个节点。 节点分两种： - agent: 通过 A2A 调 agent_manager.get(name).handle(msg) - system: 通过 ServiceRegistry.dispatch(system_action, params, tenant_id) params 字段支持 ${var} 占位符，渲染时从 payload 取。
   方法：
   - validate(self) -> None
1. class Workflow：工作流定义（DAG）。 边由节点的 depends_on 字段推导；engine 不单独维护边。
   方法：
   - validate(self) -> None
1. class AgentManagerProtocol(Protocol)：AgentManager 抽象接口（4c 用 Protocol，4d 提供实现）。
   方法：
   - async get(self, name: str, tenant_id: str) -> Any
   - list_for_tenant(self, tenant_id: str) -> list[Any]
   - async shutdown(self) -> None
1. class WorkflowEngine：工作流注册表 + 执行器。
   方法：
   - __init__(self) -> None
   - register(self, wf: Workflow) -> None
   - get(self, name: str) -> Workflow
   - list_names(self) -> list[str]
   - list_workflows(self) -> list[Workflow]
   - get_run(self, run_id: str) -> dict | None
   - async execute(self, wf: Workflow, payload: dict, tenant_id: str, agent_manager: AgentManagerProtocol) -> str  # 执行工作流，返回 run_id。
   - _render_params(params: dict, payload: dict) -> dict  # ${var} → payload[var]；缺失抛 ValueError。
   - _topo_layers(nodes: list[TaskSpec]) -> list[list[TaskSpec]]  # Kahn 算法按入度分层，返回可并行的层序列表。
   - async _dispatch(self, node: TaskSpec, params: dict, tenant_id: str, agent_manager: AgentManagerProtocol, run_id: str) -> Any
   - async _dispatch_agent(self, node: TaskSpec, params: dict, tenant_id: str, agent_manager: AgentManagerProtocol, run_id: str) -> Any
   - async _dispatch_system(self, params: dict, tenant_id: str, run_id: str, node: TaskSpec) -> Any
   - _register_default_workflows(self) -> None

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================