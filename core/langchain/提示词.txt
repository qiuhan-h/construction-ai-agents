================================================================================
core/langchain/ 目录代码生成提示词合集
================================================================================
本文件汇总 core/langchain/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
core.langchain 包级 API（5b.6 实做 + 子包聚合）。 层级： - ``chains/``           — 5b.6 实做的业务链子包（对齐 shu_zhuang_tu.txt §184-188） - ``chains/__init__.py``       协议 + MockBusinessChain + LCTBusinessChain + 工厂注册表（get_chain/register_chain/list_chains） - ``chains/safety_chain.py``   安全审核链（方案/图纸/结构 → verdict/findings/suggestions） - ``chains/compliance_chain.py`` 合规校验链（法规比对 → passed/violations） - ``chains/monitoring_chain.py`` 监控分析链（告警 → action/reasoning/notify） - ``memory/``          — ConversationMemory / VectorMemory（5b.6 不动） - ``tools/``           — 5b.6 实做的业务工具子包（对齐 shu_zhuang_tu.txt §193-198） - ``tools/__init__.py``       适配层 + 工厂注册表（BusinessTool / MCPToolAdapter / LangChainToolAdapter / to_langchain_tool / get_tool） - ``tools/calculation.py``    计算工具（荷载/风险/结构校核 → MCP calculation.*） - ``tools/document_parse.py`` 文档解析工具（方案/图纸/文档校验 → MCP validation.*） - ``tools/gis_tools.py``       GIS 工具（geofence / 空间监测 → MCP analysis.*） - ``tools/vector_search.py``   向量检索工具（5b.2 ChromaDB 接入） 5b.6 入口约定： from core.langchain import get_chain, BusinessChain chain = get_chain("safety_audit") out = await chain.run(tenant_id, {"document": "..."}) from core.langchain import get_tool, to_langchain_tool biz = get_tool("calculation.load") lc_tool = to_langchain_tool(biz)

【依赖】
- from __future__ import annotations
- from core.langchain.chains import BusinessChain, ChainContext, LCTBusinessChain, MockBusinessChain, get_chain, langchain_available, list_chains, register_chain, reset_default_chains
- from core.langchain.tools import BusinessTool, LangChainToolAdapter, MCPToolAdapter, MockTool, get_business_tool, get_business_tools, get_tool, list_tools, register_tool, reset_default_tools, to_langchain_tool

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================