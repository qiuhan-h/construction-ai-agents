================================================================================
agents/compliance_agent/checkers/ 目录代码生成提示词合集
================================================================================
本文件汇总 agents/compliance_agent/checkers/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
专项检查器：fire / seismic / energy / green 4 类。

【依赖】
- from agents.compliance_agent.checkers.energy_checker import EnergyChecker
- from agents.compliance_agent.checkers.fire_checker import FireChecker
- from agents.compliance_agent.checkers.green_checker import GreenChecker
- from agents.compliance_agent.checkers.seismic_checker import SeismicChecker

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】energy_checker.py
================================================================================
你是一名 Python 后端开发者。请实现 energy_checker.py。

【背景】
节能合规核查（GB 50189）。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from common.constants import ViolationSeverity
- from core.rules.rule import ComplianceRule, compare
- from models.domain import Violation
- from agents.compliance_agent.regulation_engine.mcp_loader import RegulationSummary

【必须实现的类】
1. class EnergyChecker：节能合规核查（GB 50189 公共建筑节能设计标准）首版。
   方法：
   - __init__(self) -> None
   - _load_default_rules(self) -> list[ComplianceRule]
   - async check(self, design_doc: dict[str, Any], regulations: list[RegulationSummary] | None, tenant_id: str=None) -> list[Violation]
   - _run_rules(self, design_doc: dict[str, Any], tenant_id: str) -> list[Violation]
   - _make_violation(rule: ComplianceRule, actual: Any, tenant_id: str) -> Violation

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 3】fire_checker.py
================================================================================
你是一名 Python 后端开发者。请实现 fire_checker.py。

【背景】
防火合规核查（GB 50016）。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from common.constants import ViolationSeverity
- from core.rules.rule import ComplianceRule, compare
- from models.domain import Violation
- from agents.compliance_agent.regulation_engine.mcp_loader import RegulationSummary

【必须实现的类】
1. class FireChecker：防火合规核查（GB 50016）首版。
   方法：
   - __init__(self) -> None
   - _load_default_rules(self) -> list[ComplianceRule]
   - async check(self, design_doc: dict[str, Any], regulations: list[RegulationSummary] | None, tenant_id: str=None) -> list[Violation]  # 对 design_doc 执行所有 fire 规则；返回违规项列表。
   - _run_rules(self, design_doc: dict[str, Any], tenant_id: str) -> list[Violation]
   - _make_violation(rule: ComplianceRule, actual: Any, tenant_id: str) -> Violation

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 4】green_checker.py
================================================================================
你是一名 Python 后端开发者。请实现 green_checker.py。

【背景】
绿色建筑合规核查（GB/T 50378）。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from common.constants import ViolationSeverity
- from core.rules.rule import ComplianceRule, compare
- from models.domain import Violation
- from agents.compliance_agent.regulation_engine.mcp_loader import RegulationSummary

【必须实现的类】
1. class GreenChecker：绿色建筑合规核查（GB/T 50378）首版。
   方法：
   - __init__(self) -> None
   - _load_default_rules(self) -> list[ComplianceRule]
   - async check(self, design_doc: dict[str, Any], regulations: list[RegulationSummary] | None, tenant_id: str=None) -> list[Violation]
   - _run_rules(self, design_doc: dict[str, Any], tenant_id: str) -> list[Violation]
   - _make_violation(rule: ComplianceRule, actual: Any, tenant_id: str) -> Violation

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 5】seismic_checker.py
================================================================================
你是一名 Python 后端开发者。请实现 seismic_checker.py。

【背景】
抗震合规核查（GB 50011）。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from common.constants import ViolationSeverity
- from core.rules.rule import ComplianceRule, compare
- from models.domain import Violation
- from agents.compliance_agent.regulation_engine.mcp_loader import RegulationSummary

【必须实现的类】
1. class SeismicChecker：抗震合规核查（GB 50011）首版。
   方法：
   - __init__(self) -> None
   - _load_default_rules(self) -> list[ComplianceRule]
   - async check(self, design_doc: dict[str, Any], regulations: list[RegulationSummary] | None, tenant_id: str=None) -> list[Violation]
   - _run_rules(self, design_doc: dict[str, Any], tenant_id: str) -> list[Violation]
   - _make_violation(rule: ComplianceRule, actual: Any, tenant_id: str) -> Violation

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================