================================================================================
agents/compliance_agent/ 目录代码生成提示词合集
================================================================================
本文件汇总 agents/compliance_agent/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
施工合规校验智能体（compliance_agent）。 公开 API： - ComplianceAgent                主类（A2A BaseAgent 子类） - ComplianceLangChainAgent        LangChain 风格入口 - ComplianceMCPHandler            MCP 业务处理器 - build_a2a_router                 构造 FastAPI A2A 路由 - install()                        一次性安装 MCP 工具并返回 handler 使用： from agents.compliance_agent import ( ComplianceAgent, make_compliance_agent, ) from agents.compliance_agent.mcp_handlers import install agent = make_compliance_agent(tenant_id="tnt_x") install()  # 把 compliance.search_regulation / list_violations 等注册到 MCP

【依赖】
- from agents.compliance_agent.agent import ComplianceAgent, make_compliance_agent
- from agents.compliance_agent.langchain_agent import ComplianceLangChainAgent
- from agents.compliance_agent.prompts import PROMPT_ENERGY_REVIEW, PROMPT_FIRE_REVIEW, PROMPT_GREEN_REVIEW, PROMPT_SEISMIC_REVIEW

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】a2a_handlers.py
================================================================================
你是一名 Python 后端开发者。请实现 a2a_handlers.py。

【背景】
施工合规校验智能体的 A2A 处理器：注册与路由挂载。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from common.exceptions import A2AAgentNotFoundError
- from core.a2a.server import A2AServer, AgentRegistry, get_registry, reset_registry
- from agents.compliance_agent.agent import ComplianceAgent

【必须实现的函数】
1. register_compliance_agent(agent: ComplianceAgent, registry: AgentRegistry | None=None) -> AgentRegistry  # 把 ComplianceAgent 注册到 AgentRegistry。
2. build_a2a_router(agent: ComplianceAgent, registry: AgentRegistry | None=None)  # 构造 A2A 路由（FastAPI Router）。 使用方式： app.include_router(build_a2a_router(agent), prefix="/api/v1/a2a")

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 3】agent.py
================================================================================
你是一名 Python 后端开发者。请实现 agent.py。

【背景】
施工合规校验智能体主类（ComplianceAgent）。

【依赖】
- from __future__ import annotations
- import asyncio
- import logging
- from typing import Any
- from common.constants import AgentName
- from common.exceptions import AgentParseError, AppException
- from common.ids import event_id, message_id
- from common.timeutils import to_iso, utc_now
- from core.a2a.agent_card import Skill
- from core.a2a.message import A2AMessage, MessagePart, Task
- from core.events import Event, get_event_bus
- from agents.base_agent import BaseAgent, register_agent
- from agents.compliance_agent.checkers import EnergyChecker, FireChecker, GreenChecker, SeismicChecker
- from agents.compliance_agent.outputs import ComplianceReporter, ReportArtifacts, ViolationRepository, ViolationTracker
- from agents.compliance_agent.prompts import list_prompts as _list_compliance_prompts
- ...（共 18 个导入）

【必须实现的类】
1. class ComplianceAgent(BaseAgent)：施工合规校验智能体。
   方法：
   - __init__(self, tenant_id: str, a2a_url: str, card_path: str, metadata: dict[str, Any] | None, signer_secret: str | None, session_factory: Any | None, auto_register: bool) -> None
   - async handle(self, message: A2AMessage, task: Task) -> A2AMessage
   - _extract_payload(self, message: A2AMessage) -> dict[str, Any]
   - async _run_compliance(self, payload: dict[str, Any]) -> ReportArtifacts
   - _build_reply(self, artifacts: ReportArtifacts, src: A2AMessage) -> A2AMessage
   - _error_reply(self, src: A2AMessage, reason: str) -> A2AMessage
   - async _publish_event_safe(self, topic: str, payload: dict[str, Any]) -> None

【必须实现的函数】
1. make_compliance_agent(tenant_id: str, a2a_url: str, signer_secret: str | None, session_factory: Any | None) -> ComplianceAgent

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 4】langchain_agent.py
================================================================================
你是一名 Python 后端开发者。请实现 langchain_agent.py。

【背景】
施工合规校验智能体的 LangChain 风格入口（最小实现）。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from core.a2a.message import A2AMessage, Task
- from agents.compliance_agent.agent import ComplianceAgent

【必须实现的类】
1. class ComplianceLangChainAgent：LangChain 风格的合规智能体入口（包装 ComplianceAgent）。
   方法：
   - __init__(self, base_agent: ComplianceAgent) -> None
   - async run(self, message: A2AMessage, task: Task) -> A2AMessage  # 统一入口：直接走 ComplianceAgent.handle()。
   - base_agent(self) -> ComplianceAgent
   - has_langchain(self) -> bool

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 5】mcp_handlers.py
================================================================================
你是一名 Python 后端开发者。请实现 mcp_handlers.py。

【背景】
施工合规校验智能体的 MCP 处理器：桥接 + 业务工具。

【依赖】
- from __future__ import annotations
- import logging
- from typing import Any
- from common.exceptions import AgentError, AgentParseError
- from core.llm import get_prompt_manager
- from core.mcp.server import MCPService, get_mcp_service
- from agents.compliance_agent.prompts import PROMPT_ENERGY_REVIEW, PROMPT_FIRE_REVIEW, PROMPT_GREEN_REVIEW, PROMPT_SEISMIC_REVIEW
- from agents.compliance_agent.regulation_engine import RegulationIndex, RegulationLoader
- from agents.compliance_agent.outputs import ViolationRepository

【必须实现的类】
1. class ComplianceMCPHandler：合规智能体的 MCP 业务处理器。
   方法：
   - __init__(self, service: MCPService | None=None) -> None
   - async read_regulation(self, code: str, version: str) -> dict[str, Any]
   - async search_regulation(self, design_type: str, top_k: int) -> dict[str, Any]  # 按 design_type 查适用法规清单。
   - async list_violations(self, tenant_id: str, status: str | None, limit: int) -> dict[str, Any]  # 按 tenant_id + 状态查违规项。首版 mock 模式。
   - async mark_rectified(self, violation_id: str, note: str) -> dict[str, Any]  # 标记违规已整改（首版 mock）。
   - async invoke_review(self, tenant_id: str, project_id: str, plan_id: str, design_doc: dict[str, Any], checks: dict[str, bool] | None) -> dict[str, Any]  # 供 4c 编排器调用：触发复审流程。 返回：调起 ComplianceAgent.handle 的元数据（实际执行由编排器完成）。
   - async render_prompt(self, name: str, project: str, design_doc: dict[str, Any], regulations: str='') -> dict[str, Any]  # 渲染 compliance.* 提示词。

【必须实现的函数】
1. _summarize(d: dict[str, Any]) -> str  # 把 design_doc 简化为单行摘要（避免提示词模板被超长文本填满）。
2. register_compliance_tools(service: MCPService | None=None) -> None  # 把 compliance 业务工具注册到 MCP 服务。
3. install() -> ComplianceMCPHandler  # 安装 compliance_agent 的 MCP 工具，返回 handler。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 6】prompts.py
================================================================================
你是一名 Python 后端开发者。请实现 prompts.py。

【背景】
合规校验智能体专用提示词模板。

【依赖】
- from __future__ import annotations
- import logging
- from core.llm import PromptTemplate, get_prompt_manager

【必须实现的函数】
1. _register_all() -> None
2. list_prompts() -> list[str]

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================