================================================================================
tests/safety_audit_agent/ 目录代码生成提示词合集
================================================================================
本文件汇总 tests/safety_audit_agent/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
safety_audit_agent 端到端自检测试包。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】test_agent.py
================================================================================
你是一名 Python 后端开发者。请实现 test_agent.py。

【背景】
阶段三（safety_audit_agent）端到端自检。 执行方式： cd construction-ai-agents python tests/safety_audit_agent/test_agent.py 覆盖方案解析 / 荷载组合 / LEC 风险评估 / 结构校核 / 向量库余弦相似度 （P0-4 回归）/ 案例检索 / 报告签章 / 整改建议 / 报告生成 / Agent 端到端 （A2A → 签章 → 事件）/ MCP 工具与提示词注册，共 11 项。 全部用例不依赖外部 LLM / DB / broker；向量库走内存，签章用测试密钥。 可直接 ``python`` 运行，也可被 pytest 收集（自检仅在 __main__ 下执行， 避免模块级 sys.exit 触发 INTERNALERROR）。

【依赖】
- from __future__ import annotations
- import asyncio
- import sys
- import traceback
- from pathlib import Path

【关键常量】
- ROOT
- _SELF_TEST_CASES = [('a) PlanParser 方案文本解析', t_plan_parser), ('b) LoadCalculator GB50009 荷载组合', t_l

【必须实现的函数】
1. check(name: str, fn) -> None
2. t_plan_parser() -> None
3. t_load_calculator() -> None
4. t_lec_assessor() -> None
5. t_structural_analyzer() -> None
6. t_vector_store_cosine() -> None
7. t_case_retriever() -> None
8. t_report_signer() -> None
9. t_recommendation_engine() -> None
10. t_report_generator() -> None
11. t_agent_end_to_end() -> None
12. t_mcp_and_prompts() -> None
13. _run_self_test() -> int  # 脚本式自检：逐条运行用例，返回退出码（0=全过，1=有失败）。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================