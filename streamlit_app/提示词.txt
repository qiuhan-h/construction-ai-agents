================================================================================
streamlit_app/ 目录代码生成提示词合集
================================================================================
本文件汇总 streamlit_app/ 目录下所有代码文件的"代码生成提示词"。
每一段都是一段独立的 prompt，可直接复制粘贴给大模型，让其生成对应代码文件。
分段处理，每段对应一个代码文件。生成代码时请严格遵守每段中的接口契约和约束。

================================================================================
【生成文件 1】__init__.py
================================================================================
你是一名 Python 后端开发者。请实现 __init__.py。

【背景】
Streamlit 前端包入口（4e）。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
【生成文件 2】app.py
================================================================================
你是一名 Python 后端开发者。请实现 app.py。

【背景】
Streamlit 多页应用入口（4e）。 布局： - sidebar：环境配置（API base / token / 租户 / 用户）→ 一键保存到 session_state； - 顶部 banner：当前 app 名 + 版本 + health 状态（按钮触发 refresh）； - 主体：pages/ 下的 6 个页面通过顶部按钮切换（4e 默认停在 04_dashboard）。 设计： - 不依赖 orchestrator 内部模块，仅通过 ``api_client`` 与后端交互； - 4e 仅实现 04_dashboard，其它 5 个 page 留占位文本（5a 阶段实做）。

【依赖】
- from __future__ import annotations
- import streamlit as st
- from streamlit_app.utils import APIError, api_client, get_state, init_state

【关键常量】
- APP_NAME = '建筑工程智能体集群'
- APP_VERSION = '0.4.0-e'
- PAGES = [{'id': '01_safety_audit', 'title': '01 安全审核', 'icon': '🛡️'}, {'id': '02_complia

【必须实现的函数】
1. _render_sidebar(state) -> None  # 渲染左侧配置面板。
2. _check_health(only: str) -> None
3. _route() -> None  # 侧边栏选择路由到对应页面。
4. _render_active_page() -> None
5. main() -> None
6. _inject_pwa() -> None  # 注入 PWA 相关的 HTML 头元素（manifest + 移动端 meta）。

【约束】
- 严格遵循上述接口签名（参数名、类型注解、返回值类型）
- 保持向后兼容，不修改已有公共接口
- 新增功能须补充对应单元测试

【输出】
完整的代码文件，使用中文 docstring 和类型注解，并在关键逻辑处添加中文行内注释。

================================================================================
目录级生成约束
================================================================================
1. 所有文件必须使用中文 docstring 和类型注解（from __future__ import annotations）
2. 严格遵循各文件【必须实现的类/函数】中定义的接口签名
3. 新增功能须补充对应单元测试，运行 pytest 确认无回归
4. 涉及环境变量的配置须同步更新 .env.example 与 config/settings.py

【中文注释规范】（所有生成的代码必须遵守）
- 模块顶部 docstring：中文说明文件职责、设计原则
- 类 docstring：中文说明类用途、关键属性
- 方法/函数 docstring：中文说明功能、参数含义、返回值
- 关键代码逻辑：必须添加中文行内注释（# 注释），解释"为什么这样做"
- 复杂分支、循环、异常处理：必须有中文注释说明意图
- 禁止使用英文注释；禁止无注释的复杂逻辑块
- 常量、枚举、配置项：必须有中文注释说明用途
================================================================================